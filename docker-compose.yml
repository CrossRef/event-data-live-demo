version: '2'
services:
  demo:
    depends_on:
     - redis
    build: .
    ports:
     - '8101:8101'
    volumes:
     - .:/usr/src/app
     - ./.m2-cache:/home/root/.m2
    environment:
     - PORT=8101
     - REDIS_HOST=redis
     - REDIS_PORT=6379
     - REDIS_DB=0
     - JWT_SECRETS=TEST,TEST2
     - STATUS_SERVICE=http://138.201.202.133:8003

  redis:
    image: redis
